# ============================================================
# Runtime dependencies
# ============================================================

fastapi==0.115.5
uvicorn==0.32.1
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1

# LLM Integration Layer
google-genai>=1.19.0          # Gemini provider (GeminiAdapter)
httpx>=0.28.0                 # HTTP client for LocalLlamaAdapter

# ============================================================
# Future — add when the relevant sprint begins
# ============================================================
# sqlalchemy==2.0.36       # PostgreSQL ORM
# asyncpg==0.30.0          # Async PostgreSQL driver
# alembic==1.14.0          # DB migrations
# boto3==1.35.79           # AWS CloudWatch + Bedrock
